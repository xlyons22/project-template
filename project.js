title('Clown Killer');

// put variables here, just like kahn academy
var exampleVar = 100;

// put functions here, just like kahn academy
var drawclown = function() {
	fill(23,32,3);
	ellipse(400,600,150,42);
	ellipse(400,543,75,75);
	noStroke();
	fill(250,255,250);
	ellipse(400,543,50,50);
	ellipse(400,510,40,40);
	ellipse(400,480,30,30);
	ellipse
	ellipse
	ellipse

};

// this code is executed once when the program is started
var setup = function() {

    // set up the size of the canvas (you probably don't want to change this!)
    size(800, 600);
};

// override draw function, by default it will be called 60 times per second
var draw = function() {

    // clear the screen
    background(240,10,10);
    // some drawing commands; feel free to change these
    fill(255, 320,0)
    // call a functon (defined up above)
    drawclown();
	

};
